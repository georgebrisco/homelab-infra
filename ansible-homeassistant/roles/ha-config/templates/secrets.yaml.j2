# Home Assistant secrets — managed by Ansible Vault
# Do not edit on the HA host directly; changes will be overwritten.
# Source: ansible-homeassistant/roles/ha-config/templates/secrets.yaml.j2
#
# Add new secrets to group_vars/all/homeassistant_vault.yml (encrypted),
# then reference them here as Ansible variables.

mqtt_user: "{{ mqtt_user }}"
mqtt_pass: "{{ mqtt_pass }}"
